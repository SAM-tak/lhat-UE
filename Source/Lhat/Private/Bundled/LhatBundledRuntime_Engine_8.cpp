// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Kismet/KismetMathLibrary.h"
#include "Kismet/KismetStringLibrary.h"
#include "Kismet/KismetSystemLibrary.h"
#include "UObject/Object.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FTransform Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Transform")) return;
    const auto Result = UKismetMathLibrary::EqualEqual_TransformTransform(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FTransform Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Transform")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetMathLibrary::NearlyEqual_TransformTransform(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FTransform Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Transform")) return;
    const auto Result = UKismetMathLibrary::ComposeTransforms(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::TransformLocation(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::TransformDirection(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    const auto Result = UKismetMathLibrary::TransformRotation(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::InverseTransformLocation(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::InverseTransformDirection(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    const auto Result = UKismetMathLibrary::InverseTransformRotation(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FTransform Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Transform")) return;
    const auto Result = UKismetMathLibrary::MakeRelativeTransform(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    const auto Result = UKismetMathLibrary::InvertTransform(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FTransform Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Transform")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::TInterpTo(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    const auto Result = UKismetMathLibrary::Transform_Determinant(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    double Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    double Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::FInterpTo(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    double Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    double Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::FInterpTo_Constant(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Rotator")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::RInterpTo(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Rotator")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::RInterpTo_Constant(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::PerlinNoise1D(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    const auto Result = UKismetMathLibrary::IsPointInBox(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FTransform Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Transform")) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    const auto Result = UKismetMathLibrary::IsPointInBoxWithTransform(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::WeightedMovingAverage_Float(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::WeightedMovingAverage_FVector(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Rotator")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::WeightedMovingAverage_FRotator(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetMathLibrary::DynamicWeightedMovingAverage_Float(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetMathLibrary::DynamicWeightedMovingAverage_FVector(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Rotator")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetMathLibrary::DynamicWeightedMovingAverage_FRotator(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_DoubleToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_IntToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_Int64ToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_ByteToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_BoolToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    const auto Result = UKismetStringLibrary::Conv_VectorToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector2D Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector2D")) return;
    const auto Result = UKismetStringLibrary::Conv_Vector2dToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Rotator")) return;
    const auto Result = UKismetStringLibrary::Conv_RotatorToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    const auto Result = UKismetStringLibrary::Conv_TransformToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_ObjectToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetStringLibrary::Conv_ColorToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_NameToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_StringToName(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_StringToInt(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_StringToInt64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Conv_StringToDouble(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    double Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Double(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Int(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Bool(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Vector(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FVector2D Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector2D")) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Vector2d(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FRotator Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Rotator")) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Rotator(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UObject* Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Object(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FLinearColor Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "LinearColor")) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Color(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FName Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::BuildString_Name(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::Concat_StrStr(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::EqualEqual_StrStr(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::EqualEqual_StriStri(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::NotEqual_StrStr(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::NotEqual_StriStri(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Len(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::IsEmpty(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetStringLibrary::GetSubstring(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    int32 Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetStringLibrary::FindSubstring(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetStringLibrary::Contains(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::GetCharacterAsNumber(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::ToUpper(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::ToLower(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::LeftPad(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::RightPad(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::IsNumeric(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Trim(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::TrimTrailing(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::Reverse(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::Left(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::LeftChop(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::Right(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::RightChop(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetStringLibrary::Mid(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetStringLibrary::TimeSecondsToString(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UKismetSystemLibrary::RaiseScriptError(Arg0);
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetSystemLibrary::GetObjectName(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetSystemLibrary::GetPathName(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetSystemLibrary::GetSystemPath(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
}
void Gather_LhatBundledRuntime_Engine_8(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledRuntime_Engine_8(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_TransformTransform", "f^ue.Transform, ue.Transform -> bool^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NearlyEqual_TransformTransform", "f^ue.Transform, ue.Transform, number^, number^, number^ -> bool^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "ComposeTransforms", "f^ue.Transform, ue.Transform -> ue.Transform;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "TransformLocation", "f^ue.Transform, ue.Vector -> ue.Vector;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "TransformDirection", "f^ue.Transform, ue.Vector -> ue.Vector;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "TransformRotation", "f^ue.Transform, ue.Rotator -> ue.Rotator;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "InverseTransformLocation", "f^ue.Transform, ue.Vector -> ue.Vector;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "InverseTransformDirection", "f^ue.Transform, ue.Vector -> ue.Vector;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "InverseTransformRotation", "f^ue.Transform, ue.Rotator -> ue.Rotator;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "MakeRelativeTransform", "f^ue.Transform, ue.Transform -> ue.Transform;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "InvertTransform", "f^ue.Transform -> ue.Transform;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "TInterpTo", "f^ue.Transform, ue.Transform, number^, number^ -> ue.Transform;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Transform_Determinant", "f^ue.Transform -> number^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "FInterpTo", "f^number^, number^, number^, number^ -> number^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "FInterpTo_Constant", "f^number^, number^, number^, number^ -> number^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RInterpTo", "f^ue.Rotator, ue.Rotator, number^, number^ -> ue.Rotator;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RInterpTo_Constant", "f^ue.Rotator, ue.Rotator, number^, number^ -> ue.Rotator;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "PerlinNoise1D", "f^number^ -> number^;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "IsPointInBox", "f^ue.Vector, ue.Vector, ue.Vector -> bool^;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "IsPointInBoxWithTransform", "f^ue.Vector, ue.Transform, ue.Vector -> bool^;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "WeightedMovingAverage_Float", "f^number^, number^, number^ -> number^;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "WeightedMovingAverage_FVector", "f^ue.Vector, ue.Vector, number^ -> ue.Vector;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "WeightedMovingAverage_FRotator", "f^ue.Rotator, ue.Rotator, number^ -> ue.Rotator;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "DynamicWeightedMovingAverage_Float", "f^number^, number^, number^, number^, number^ -> number^;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "DynamicWeightedMovingAverage_FVector", "f^ue.Vector, ue.Vector, number^, number^, number^ -> ue.Vector;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "DynamicWeightedMovingAverage_FRotator", "f^ue.Rotator, ue.Rotator, number^, number^, number^ -> ue.Rotator;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_DoubleToString", "f^number^ -> string^;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_IntToString", "f^number^ -> string^;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_Int64ToString", "f^number^ -> string^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_ByteToString", "f^number^ -> string^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_BoolToString", "f^bool^ -> string^;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_VectorToString", "f^ue.Vector -> string^;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_Vector2dToString", "f^ue.Vector2D -> string^;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_RotatorToString", "f^ue.Rotator -> string^;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_TransformToString", "f^ue.Transform -> string^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_ObjectToString", "f^ue.Object|nil^ -> string^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_ColorToString", "f^ue.LinearColor -> string^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_NameToString", "f^string^ -> string^;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_StringToName", "f^string^ -> string^;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_StringToInt", "f^string^ -> number^;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_StringToInt64", "f^string^ -> number^;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Conv_StringToDouble", "f^string^ -> number^;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Double", "f^string^, string^, number^, string^ -> string^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Int", "f^string^, string^, number^, string^ -> string^;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Bool", "f^string^, string^, bool^, string^ -> string^;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Vector", "f^string^, string^, ue.Vector, string^ -> string^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Vector2d", "f^string^, string^, ue.Vector2D, string^ -> string^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Rotator", "f^string^, string^, ue.Rotator, string^ -> string^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Object", "f^string^, string^, ue.Object|nil^, string^ -> string^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Color", "f^string^, string^, ue.LinearColor, string^ -> string^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "BuildString_Name", "f^string^, string^, string^, string^ -> string^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Concat_StrStr", "f^string^, string^ -> string^;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "EqualEqual_StrStr", "f^string^, string^ -> bool^;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "EqualEqual_StriStri", "f^string^, string^ -> bool^;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "NotEqual_StrStr", "f^string^, string^ -> bool^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "NotEqual_StriStri", "f^string^, string^ -> bool^;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Len", "f^string^ -> number^;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "IsEmpty", "f^string^ -> bool^;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "GetSubstring", "f^string^, number^, number^ -> string^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "FindSubstring", "f^string^, string^, bool^, bool^, number^ -> number^;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Contains", "f^string^, string^, bool^, bool^ -> bool^;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "GetCharacterAsNumber", "f^string^, number^ -> number^;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "ToUpper", "f^string^ -> string^;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "ToLower", "f^string^ -> string^;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "LeftPad", "f^string^, number^ -> string^;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "RightPad", "f^string^, number^ -> string^;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "IsNumeric", "f^string^ -> bool^;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Trim", "f^string^ -> string^;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "TrimTrailing", "f^string^ -> string^;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Reverse", "f^string^ -> string^;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Left", "f^string^, number^ -> string^;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "LeftChop", "f^string^, number^ -> string^;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Right", "f^string^, number^ -> string^;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "RightChop", "f^string^, number^ -> string^;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "Mid", "f^string^, number^, number^ -> string^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "TimeSecondsToString", "f^number^ -> string^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "RaiseScriptError", "p^string^;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "GetObjectName", "f^ue.Object|nil^ -> string^;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "GetPathName", "f^ue.Object|nil^ -> string^;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "GetSystemPath", "f^ue.Object|nil^ -> string^;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
