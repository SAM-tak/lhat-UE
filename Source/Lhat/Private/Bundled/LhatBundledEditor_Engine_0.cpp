#include "CoreMinimal.h"
#if WITH_EDITOR
// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "GameFramework/Actor.h"
#include "Kismet/KismetStringLibrary.h"
#include "Kismet/KismetSystemLibrary.h"
#include "UObject/Object.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsHiddenEd();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetIsTemporarilyHiddenInEditor(Arg0);
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->IsTemporarilyHiddenInEditor(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsEditable();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsSelectable();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetDefaultActorLabel();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetActorLabel(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetActorLabel(Arg0, Arg1);
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetFolderPath();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetFolderPath(Arg0);
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetStringLibrary::DiffString(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetSystemLibrary::IsObjectCooked(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetSystemLibrary::ObjectHasEditorOnlyData(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UObject* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FName Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetSystemLibrary::DuplicateObject(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetSystemLibrary::GetPlatformTime_Seconds();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UKismetSystemLibrary::QuitEditor();
}
}
void Gather_LhatBundledEditor_Engine_0(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledEditor_Engine_0(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue", "Actor", "IsHiddenEd", "f^self^ -> bool^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetIsTemporarilyHiddenInEditor", "p^self^, bool^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "IsTemporarilyHiddenInEditor", "f^self^, bool^ -> bool^;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "IsEditable", "f^self^ -> bool^;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "IsSelectable", "f^self^ -> bool^;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetDefaultActorLabel", "f^self^ -> string^;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetActorLabel", "f^self^, bool^ -> string^;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetActorLabel", "p^self^, string^, bool^;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetFolderPath", "f^self^ -> string^;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetFolderPath", "p^self^, string^;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetStringLibrary", "DiffString", "p^string^, string^ -> string^;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "IsObjectCooked", "f^ue.Object|nil^ -> bool^;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "ObjectHasEditorOnlyData", "f^ue.Object|nil^ -> bool^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "DuplicateObject", "p^ue.Object|nil^, ue.Object|nil^, string^ -> ue.Object|nil^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "GetPlatformTime_Seconds", "p^ -> number^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetSystemLibrary", "QuitEditor", "p^;", Call15, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
#endif
