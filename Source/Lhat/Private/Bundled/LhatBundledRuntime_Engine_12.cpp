// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Components/PrimitiveComponent.h"
#include "Components/SceneComponent.h"
#include "GameFramework/Pawn.h"
#include "PhysicalMaterials/PhysicalMaterial.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetOnlyOwnerSee(Arg0);
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetVisibleInRayTracing(Arg0);
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetCastShadow(Arg0);
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetEmissiveLightSource(Arg0);
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetCastHiddenShadow(Arg0);
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetCastInsetShadow(Arg0);
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetCastContactShadow(Arg0);
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetLightAttachmentsAsGroup(Arg0);
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetExcludeFromLightAttachmentGroup(Arg0);
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetSingleSampleShadowFromStationaryLights(Arg0);
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetTranslucentSortPriority(Arg0);
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetTranslucencySortDistanceOffset(Arg0);
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAffectDistanceFieldLighting(Arg0);
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetReceivesDecals(Arg0);
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetHoldout(Arg0);
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAffectDynamicIndirectLighting(Arg0);
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAffectIndirectLightingWhileHidden(Arg0);
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetCollisionProfileName(Arg0, Arg1);
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetCollisionProfileName();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetRenderCustomDepth(Arg0);
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetCustomDepthStencilValue(Arg0);
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetRenderInMainPass(Arg0);
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetRenderInDepthPass(Arg0);
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetVisibleInSceneCaptureOnly(Arg0);
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetHiddenInSceneCapture(Arg0);
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetNumMaterials();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetAllPhysicsAngularVelocityInRadians(Arg0, Arg1);
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    Self->WakeAllRigidBodies();
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetEnableGravity(Arg0);
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsGravityEnabled();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetUpdateKinematicFromSimulation(Arg0);
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetUpdateKinematicFromSimulation();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetGyroscopicTorqueEnabled(Arg0);
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetGyroscopicTorqueEnabled();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetLinearDamping(Arg0);
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetLinearDamping();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAngularDamping(Arg0);
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetAngularDamping();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetMassScale(Arg0, Arg1);
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetMassScale(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAllMassScale(Arg0);
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetMassOverrideInKg(Arg0, Arg1, Arg2);
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMass();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetInertiaTensor(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    const auto Result = Self->ScaleByMomentOfInertia(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetMaxDepenetrationVelocity(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetMaxDepenetrationVelocity(Arg0, Arg1);
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetUseCCD(Arg0, Arg1);
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAllUseCCD(Arg0);
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetUseMACD(Arg0, Arg1);
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAllUseMACD(Arg0);
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetAllowPartialIslandSleep(Arg0, Arg1);
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAllAllowPartialIslandSleep(Arg0);
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsAnyRigidBodyAwake();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    Self->RegisterAsFocalPointInPhysicsReplicationLOD();
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    Self->UnregisterAsFocalPointInPhysicsReplicationLOD();
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    UPhysicalMaterial* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetPhysMaterialOverride(Arg0);
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetCullDistance(Arg0);
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UPrimitiveComponent*>(B.ReadObject(M, Args[0], UPrimitiveComponent::StaticClass()));
    if (!Self) return;
    APawn* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->CanCharacterStepUp(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetShouldUpdatePhysicsVolume(Arg0);
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetRelativeTransform();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    Self->ResetRelativeTransform();
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    Self->SetRelativeScale3D(Arg0);
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    Self->SetWorldScale3D(Arg0);
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->K2_GetComponentLocation();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->K2_GetComponentRotation();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->K2_GetComponentScale();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->K2_GetComponentToWorld();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetForwardVector();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetUpVector();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetRightVector();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->IsSimulatingPhysics(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsAnySimulatingPhysics();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetNumChildrenComponents();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetChildComponent(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->DetachFromParent(Arg0, Arg1);
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetSocketLocation(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetSocketRotation(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetSocketQuaternion(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Quat")) *AnswerCount = 1;
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->DoesSocketExist(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
}
void Gather_LhatBundledRuntime_Engine_12(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledRuntime_Engine_12(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetOnlyOwnerSee", "p^self^, bool^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetVisibleInRayTracing", "p^self^, bool^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetCastShadow", "p^self^, bool^;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetEmissiveLightSource", "p^self^, bool^;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetCastHiddenShadow", "p^self^, bool^;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetCastInsetShadow", "p^self^, bool^;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetCastContactShadow", "p^self^, bool^;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetLightAttachmentsAsGroup", "p^self^, bool^;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetExcludeFromLightAttachmentGroup", "p^self^, bool^;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetSingleSampleShadowFromStationaryLights", "p^self^, bool^;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetTranslucentSortPriority", "p^self^, number^;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetTranslucencySortDistanceOffset", "p^self^, number^;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAffectDistanceFieldLighting", "p^self^, bool^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetReceivesDecals", "p^self^, bool^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetHoldout", "p^self^, bool^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAffectDynamicIndirectLighting", "p^self^, bool^;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAffectIndirectLightingWhileHidden", "p^self^, bool^;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetCollisionProfileName", "p^self^, string^, bool^;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetCollisionProfileName", "f^self^ -> string^;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetRenderCustomDepth", "p^self^, bool^;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetCustomDepthStencilValue", "p^self^, number^;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetRenderInMainPass", "p^self^, bool^;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetRenderInDepthPass", "p^self^, bool^;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetVisibleInSceneCaptureOnly", "p^self^, bool^;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetHiddenInSceneCapture", "p^self^, bool^;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetNumMaterials", "f^self^ -> number^;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAllPhysicsAngularVelocityInRadians", "p^self^, ue.Vector, bool^;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "WakeAllRigidBodies", "p^self^;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetEnableGravity", "p^self^, bool^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "IsGravityEnabled", "f^self^ -> bool^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetUpdateKinematicFromSimulation", "p^self^, bool^;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetUpdateKinematicFromSimulation", "f^self^ -> bool^;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetGyroscopicTorqueEnabled", "p^self^, bool^;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetGyroscopicTorqueEnabled", "f^self^ -> bool^;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetLinearDamping", "p^self^, number^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetLinearDamping", "f^self^ -> number^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAngularDamping", "p^self^, number^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetAngularDamping", "f^self^ -> number^;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetMassScale", "p^self^, string^, number^;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetMassScale", "f^self^, string^ -> number^;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAllMassScale", "p^self^, number^;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetMassOverrideInKg", "p^self^, string^, number^, bool^;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetMass", "f^self^ -> number^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetInertiaTensor", "f^self^, string^ -> ue.Vector;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "ScaleByMomentOfInertia", "f^self^, ue.Vector, string^ -> ue.Vector;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "GetMaxDepenetrationVelocity", "p^self^, string^ -> number^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetMaxDepenetrationVelocity", "p^self^, string^, number^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetUseCCD", "p^self^, bool^, string^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAllUseCCD", "p^self^, bool^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetUseMACD", "p^self^, bool^, string^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAllUseMACD", "p^self^, bool^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAllowPartialIslandSleep", "p^self^, bool^, string^;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetAllAllowPartialIslandSleep", "p^self^, bool^;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "IsAnyRigidBodyAwake", "f^self^ -> bool^;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "RegisterAsFocalPointInPhysicsReplicationLOD", "p^self^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "UnregisterAsFocalPointInPhysicsReplicationLOD", "p^self^;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetPhysMaterialOverride", "p^self^, ue.PhysicsCore.PhysicalMaterial|nil^;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "SetCullDistance", "p^self^, number^;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "PrimitiveComponent", "CanCharacterStepUp", "f^self^, ue.Engine.Pawn|nil^ -> bool^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "SetShouldUpdatePhysicsVolume", "p^self^, bool^;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetRelativeTransform", "f^self^ -> ue.Transform;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "ResetRelativeTransform", "p^self^;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "SetRelativeScale3D", "p^self^, ue.Vector;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "SetWorldScale3D", "p^self^, ue.Vector;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "K2_GetComponentLocation", "f^self^ -> ue.Vector;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "K2_GetComponentRotation", "f^self^ -> ue.Rotator;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "K2_GetComponentScale", "f^self^ -> ue.Vector;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "K2_GetComponentToWorld", "f^self^ -> ue.Transform;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetForwardVector", "f^self^ -> ue.Vector;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetUpVector", "f^self^ -> ue.Vector;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetRightVector", "f^self^ -> ue.Vector;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "IsSimulatingPhysics", "f^self^, string^ -> bool^;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "IsAnySimulatingPhysics", "f^self^ -> bool^;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetNumChildrenComponents", "f^self^ -> number^;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetChildComponent", "f^self^, number^ -> ue.Engine.SceneComponent|nil^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "DetachFromParent", "p^self^, bool^, bool^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetSocketLocation", "f^self^, string^ -> ue.Vector;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetSocketRotation", "f^self^, string^ -> ue.Rotator;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetSocketQuaternion", "f^self^, string^ -> ue.Quat;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "DoesSocketExist", "f^self^, string^ -> bool^;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
