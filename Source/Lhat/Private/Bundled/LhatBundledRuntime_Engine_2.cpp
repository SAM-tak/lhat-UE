// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Components/AudioComponent.h"
#include "Components/DecalComponent.h"
#include "Components/ForceFeedbackComponent.h"
#include "Engine/LevelStreaming.h"
#include "Engine/StaticMesh.h"
#include "GameFramework/Actor.h"
#include "GameFramework/ForceFeedbackAttenuation.h"
#include "GameFramework/ForceFeedbackEffect.h"
#include "GameFramework/GameModeBase.h"
#include "GameFramework/GameStateBase.h"
#include "GameFramework/PlayerController.h"
#include "GameFramework/SaveGame.h"
#include "Kismet/GameplayStatics.h"
#include "Kismet/KismetMathLibrary.h"
#include "Materials/MaterialInterface.h"
#include "Sound/ReverbEffect.h"
#include "Sound/SoundAttenuation.h"
#include "Sound/SoundBase.h"
#include "Sound/SoundClass.h"
#include "Sound/SoundConcurrency.h"
#include "Sound/SoundMix.h"
#include "UObject/Object.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetPlayerControllerFromID(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UGameplayStatics::CreatePlayer(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    APlayerController* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::RemovePlayer(Arg0, Arg1);
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    APlayerController* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetPlayerControllerID(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    APlayerController* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::SetPlayerControllerID(Arg0, Arg1);
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetStreamingLevel(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::FlushLevelStreaming(Arg0);
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UGameplayStatics::CancelAsyncLoading();
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    FString Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    UGameplayStatics::OpenLevel(Arg0, Arg1, Arg2, Arg3);
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetCurrentLevelName(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetGameMode(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetGameState(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetGlobalTimeDilation(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::SetGlobalTimeDilation(Arg0, Arg1);
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::SetGamePaused(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::IsGamePaused(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::SetForceDisableSplitscreen(Arg0, Arg1);
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::IsSplitscreenForceDisabled(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::SetEnableWorldRendering(Arg0, Arg1);
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetEnableWorldRendering(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UGameplayStatics::AreAnyListenersWithinRange(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    UGameplayStatics::SetGlobalPitchModulation(Arg0, Arg1, Arg2);
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundClass* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    UGameplayStatics::SetSoundClassDistanceScale(Arg0, Arg1, Arg2, Arg3);
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 9) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    float Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    float Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    float Arg7{};
    if (!LhatUEBindings::Read(M, B, Args[7], Arg7)) return;
    float Arg8{};
    if (!LhatUEBindings::Read(M, B, Args[8], Arg8)) return;
    UGameplayStatics::SetGlobalListenerFocusParameters(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6, Arg7, Arg8);
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 8) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundBase* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    USoundConcurrency* Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    AActor* Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    bool Arg7{};
    if (!LhatUEBindings::Read(M, B, Args[7], Arg7)) return;
    UGameplayStatics::PlaySound2D(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6, Arg7);
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 8) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundBase* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    USoundConcurrency* Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    bool Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    bool Arg7{};
    if (!LhatUEBindings::Read(M, B, Args[7], Arg7)) return;
    const auto Result = UGameplayStatics::SpawnSound2D(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6, Arg7);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 8) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundBase* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    USoundConcurrency* Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    bool Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    bool Arg7{};
    if (!LhatUEBindings::Read(M, B, Args[7], Arg7)) return;
    const auto Result = UGameplayStatics::CreateSound2D(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6, Arg7);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 11) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundBase* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    FRotator Arg3{};
    if (!LhatUEBindings::ReadValue(M, B, Args[3], Arg3, "Rotator")) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    float Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    float Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    USoundAttenuation* Arg7{};
    if (!LhatUEBindings::Read(M, B, Args[7], Arg7)) return;
    USoundConcurrency* Arg8{};
    if (!LhatUEBindings::Read(M, B, Args[8], Arg8)) return;
    AActor* Arg9{};
    if (!LhatUEBindings::Read(M, B, Args[9], Arg9)) return;
    UInitialActiveSoundParams* Arg10{};
    if (!LhatUEBindings::Read(M, B, Args[10], Arg10)) return;
    UGameplayStatics::PlaySoundAtLocation(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6, Arg7, Arg8, Arg9, Arg10);
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 10) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundBase* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    FRotator Arg3{};
    if (!LhatUEBindings::ReadValue(M, B, Args[3], Arg3, "Rotator")) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    float Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    float Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    USoundAttenuation* Arg7{};
    if (!LhatUEBindings::Read(M, B, Args[7], Arg7)) return;
    USoundConcurrency* Arg8{};
    if (!LhatUEBindings::Read(M, B, Args[8], Arg8)) return;
    bool Arg9{};
    if (!LhatUEBindings::Read(M, B, Args[9], Arg9)) return;
    const auto Result = UGameplayStatics::SpawnSoundAtLocation(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6, Arg7, Arg8, Arg9);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 9) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UForceFeedbackEffect* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    FRotator Arg3{};
    if (!LhatUEBindings::ReadValue(M, B, Args[3], Arg3, "Rotator")) return;
    bool Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    float Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    float Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    UForceFeedbackAttenuation* Arg7{};
    if (!LhatUEBindings::Read(M, B, Args[7], Arg7)) return;
    bool Arg8{};
    if (!LhatUEBindings::Read(M, B, Args[8], Arg8)) return;
    const auto Result = UGameplayStatics::SpawnForceFeedbackAtLocation(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6, Arg7, Arg8);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::SetSubtitlesEnabled(Arg0);
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UGameplayStatics::AreSubtitlesEnabled();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundMix* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::SetBaseSoundMix(Arg0, Arg1);
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    USoundBase* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::PrimeSound(Arg0);
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetActiveSpatialPluginName(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::SetActiveSpatialPluginByName(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    USoundClass* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::PrimeAllSoundsInSoundClass(Arg0);
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    USoundClass* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::UnRetainAllSoundsInSoundClass(Arg0);
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 7) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundMix* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    USoundClass* Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    float Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    bool Arg6{};
    if (!LhatUEBindings::Read(M, B, Args[6], Arg6)) return;
    UGameplayStatics::SetSoundMixClassOverride(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5, Arg6);
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundMix* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    USoundClass* Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    UGameplayStatics::ClearSoundMixClassOverride(Arg0, Arg1, Arg2, Arg3);
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundMix* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::PushSoundMixModifier(Arg0, Arg1);
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    USoundMix* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::PopSoundMixModifier(Arg0, Arg1);
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::ClearSoundMixModifiers(Arg0);
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 6) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UReverbEffect* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FName Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    float Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    UGameplayStatics::ActivateReverbEffect(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5);
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::DeactivateReverbEffect(Arg0, Arg1);
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetCurrentReverbEffect(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    UGameplayStatics::SetMaxAudioChannelsScaled(Arg0, Arg1);
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetMaxAudioChannelCount(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 6) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UMaterialInterface* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    FVector Arg3{};
    if (!LhatUEBindings::ReadValue(M, B, Args[3], Arg3, "Vector")) return;
    FRotator Arg4{};
    if (!LhatUEBindings::ReadValue(M, B, Args[4], Arg4, "Rotator")) return;
    float Arg5{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg5)) return;
    const auto Result = UGameplayStatics::SpawnDecalAtLocation(Arg0, Arg1, Arg2, Arg3, Arg4, Arg5);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    USaveGame* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UGameplayStatics::SaveGameToSlot(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::DoesSaveGameExist(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::LoadGameFromSlot(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::DeleteGameInSlot(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetWorldDeltaSeconds(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetTimeSeconds(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetUnpausedTimeSeconds(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetRealTimeSeconds(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetAudioTimeSeconds(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::EnableLiveStreaming(Arg0);
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UGameplayStatics::GetPlatformName();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UGameplayStatics::RebaseLocalOriginOntoZero(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UGameplayStatics::RebaseZeroOriginOntoLocal(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UStaticMesh* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UGameplayStatics::GrassOverlappingSphereCount(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::ParseOption(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::HasOption(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UGameplayStatics::GetIntOption(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::HasLaunchOption(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UGameplayStatics::AnnounceAccessibleString(Arg0);
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::RandomBool();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::RandomBoolWithWeight(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Not_PreBool(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::EqualEqual_BoolBool(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::NotEqual_BoolBool(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::BooleanAND(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::BooleanNAND(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::BooleanOR(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::BooleanXOR(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::BooleanNOR(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Multiply_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Divide_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
}
void Gather_LhatBundledRuntime_Engine_2(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledRuntime_Engine_2(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlayerControllerFromID", "f^ue.Object|nil^, number^ -> ue.Engine.PlayerController|nil^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "CreatePlayer", "p^ue.Object|nil^, number^, bool^ -> ue.Engine.PlayerController|nil^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "RemovePlayer", "p^ue.Engine.PlayerController|nil^, bool^;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlayerControllerID", "f^ue.Engine.PlayerController|nil^ -> number^;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetPlayerControllerID", "p^ue.Engine.PlayerController|nil^, number^;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetStreamingLevel", "f^ue.Object|nil^, string^ -> ue.Engine.LevelStreaming|nil^;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "FlushLevelStreaming", "p^ue.Object|nil^;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "CancelAsyncLoading", "p^;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "OpenLevel", "p^ue.Object|nil^, string^, bool^, string^;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetCurrentLevelName", "p^ue.Object|nil^, bool^ -> string^;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetGameMode", "f^ue.Object|nil^ -> ue.Engine.GameModeBase|nil^;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetGameState", "f^ue.Object|nil^ -> ue.Engine.GameStateBase|nil^;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetGlobalTimeDilation", "f^ue.Object|nil^ -> number^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetGlobalTimeDilation", "p^ue.Object|nil^, number^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetGamePaused", "p^ue.Object|nil^, bool^ -> bool^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "IsGamePaused", "f^ue.Object|nil^ -> bool^;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetForceDisableSplitscreen", "p^ue.Object|nil^, bool^;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "IsSplitscreenForceDisabled", "f^ue.Object|nil^ -> bool^;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetEnableWorldRendering", "p^ue.Object|nil^, bool^;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetEnableWorldRendering", "p^ue.Object|nil^ -> bool^;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "AreAnyListenersWithinRange", "p^ue.Object|nil^, ue.Vector, number^ -> bool^;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetGlobalPitchModulation", "p^ue.Object|nil^, number^, number^;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetSoundClassDistanceScale", "p^ue.Object|nil^, ue.Engine.SoundClass|nil^, number^, number^;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetGlobalListenerFocusParameters", "p^ue.Object|nil^, number^, number^, number^, number^, number^, number^, number^, number^;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "PlaySound2D", "p^ue.Object|nil^, ue.Engine.SoundBase|nil^, number^, number^, number^, ue.Engine.SoundConcurrency|nil^, ue.Actor|nil^, bool^;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SpawnSound2D", "p^ue.Object|nil^, ue.Engine.SoundBase|nil^, number^, number^, number^, ue.Engine.SoundConcurrency|nil^, bool^, bool^ -> ue.Engine.AudioComponent|nil^;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "CreateSound2D", "p^ue.Object|nil^, ue.Engine.SoundBase|nil^, number^, number^, number^, ue.Engine.SoundConcurrency|nil^, bool^, bool^ -> ue.Engine.AudioComponent|nil^;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "PlaySoundAtLocation", "p^ue.Object|nil^, ue.Engine.SoundBase|nil^, ue.Vector, ue.Rotator, number^, number^, number^, ue.Engine.SoundAttenuation|nil^, ue.Engine.SoundConcurrency|nil^, ue.Actor|nil^, ue.Engine.InitialActiveSoundParams|nil^;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SpawnSoundAtLocation", "p^ue.Object|nil^, ue.Engine.SoundBase|nil^, ue.Vector, ue.Rotator, number^, number^, number^, ue.Engine.SoundAttenuation|nil^, ue.Engine.SoundConcurrency|nil^, bool^ -> ue.Engine.AudioComponent|nil^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SpawnForceFeedbackAtLocation", "p^ue.Object|nil^, ue.Engine.ForceFeedbackEffect|nil^, ue.Vector, ue.Rotator, bool^, number^, number^, ue.Engine.ForceFeedbackAttenuation|nil^, bool^ -> ue.Engine.ForceFeedbackComponent|nil^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetSubtitlesEnabled", "p^bool^;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "AreSubtitlesEnabled", "f^ -> bool^;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetBaseSoundMix", "p^ue.Object|nil^, ue.Engine.SoundMix|nil^;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "PrimeSound", "p^ue.Engine.SoundBase|nil^;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetActiveSpatialPluginName", "p^ue.Object|nil^ -> string^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetActiveSpatialPluginByName", "p^ue.Object|nil^, string^ -> bool^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "PrimeAllSoundsInSoundClass", "p^ue.Engine.SoundClass|nil^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "UnRetainAllSoundsInSoundClass", "p^ue.Engine.SoundClass|nil^;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetSoundMixClassOverride", "p^ue.Object|nil^, ue.Engine.SoundMix|nil^, ue.Engine.SoundClass|nil^, number^, number^, number^, bool^;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "ClearSoundMixClassOverride", "p^ue.Object|nil^, ue.Engine.SoundMix|nil^, ue.Engine.SoundClass|nil^, number^;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "PushSoundMixModifier", "p^ue.Object|nil^, ue.Engine.SoundMix|nil^;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "PopSoundMixModifier", "p^ue.Object|nil^, ue.Engine.SoundMix|nil^;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "ClearSoundMixModifiers", "p^ue.Object|nil^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "ActivateReverbEffect", "p^ue.Object|nil^, ue.Engine.ReverbEffect|nil^, string^, number^, number^, number^;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "DeactivateReverbEffect", "p^ue.Object|nil^, string^;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetCurrentReverbEffect", "p^ue.Object|nil^ -> ue.Engine.ReverbEffect|nil^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SetMaxAudioChannelsScaled", "p^ue.Object|nil^, number^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetMaxAudioChannelCount", "p^ue.Object|nil^ -> number^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SpawnDecalAtLocation", "p^ue.Object|nil^, ue.Engine.MaterialInterface|nil^, ue.Vector, ue.Vector, ue.Rotator, number^ -> ue.Engine.DecalComponent|nil^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "SaveGameToSlot", "p^ue.Engine.SaveGame|nil^, string^, number^ -> bool^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "DoesSaveGameExist", "p^string^, number^ -> bool^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "LoadGameFromSlot", "p^string^, number^ -> ue.Engine.SaveGame|nil^;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "DeleteGameInSlot", "p^string^, number^ -> bool^;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetWorldDeltaSeconds", "f^ue.Object|nil^ -> number^;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetTimeSeconds", "f^ue.Object|nil^ -> number^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetUnpausedTimeSeconds", "f^ue.Object|nil^ -> number^;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetRealTimeSeconds", "f^ue.Object|nil^ -> number^;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetAudioTimeSeconds", "f^ue.Object|nil^ -> number^;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "EnableLiveStreaming", "p^bool^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlatformName", "f^ -> string^;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "RebaseLocalOriginOntoZero", "f^ue.Object|nil^, ue.Vector -> ue.Vector;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "RebaseZeroOriginOntoLocal", "f^ue.Object|nil^, ue.Vector -> ue.Vector;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GrassOverlappingSphereCount", "p^ue.Object|nil^, ue.Engine.StaticMesh|nil^, ue.Vector, number^ -> number^;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "ParseOption", "f^string^, string^ -> string^;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "HasOption", "f^string^, string^ -> bool^;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetIntOption", "f^string^, string^, number^ -> number^;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "HasLaunchOption", "f^string^ -> bool^;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "AnnounceAccessibleString", "p^string^;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RandomBool", "f^ -> bool^;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RandomBoolWithWeight", "f^number^ -> bool^;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Not_PreBool", "f^bool^ -> bool^;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_BoolBool", "f^bool^, bool^ -> bool^;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_BoolBool", "f^bool^, bool^ -> bool^;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "BooleanAND", "f^bool^, bool^ -> bool^;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "BooleanNAND", "f^bool^, bool^ -> bool^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "BooleanOR", "f^bool^, bool^ -> bool^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "BooleanXOR", "f^bool^, bool^ -> bool^;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "BooleanNOR", "f^bool^, bool^ -> bool^;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Multiply_ByteByte", "f^number^, number^ -> number^;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Divide_ByteByte", "f^number^, number^ -> number^;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
