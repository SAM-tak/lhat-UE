// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Animation/AnimCompositeBase.h"
#include "Animation/AnimInstance.h"
#include "Animation/AnimMontage.h"
#include "Animation/AnimSequenceBase.h"
#include "Animation/AnimationAsset.h"
#include "Camera/CameraActor.h"
#include "Camera/CameraShakeSourceComponent.h"
#include "Camera/PlayerCameraManager.h"
#include "Components/ActorComponent.h"
#include "Components/AudioComponent.h"
#include "Components/ChildActorComponent.h"
#include "Components/DecalComponent.h"
#include "Components/ForceFeedbackComponent.h"
#include "Components/MeshComponent.h"
#include "Components/PrimitiveComponent.h"
#include "Components/SceneComponent.h"
#include "Components/SkeletalMeshComponent.h"
#include "Components/SkinnedMeshComponent.h"
#include "Components/StaticMeshComponent.h"
#include "Engine/Blueprint.h"
#include "Engine/BlueprintCore.h"
#include "Engine/Brush.h"
#include "Engine/GameInstance.h"
#include "Engine/Level.h"
#include "Engine/LevelStreaming.h"
#include "Engine/SkeletalMesh.h"
#include "Engine/SkinnedAsset.h"
#include "Engine/StaticMesh.h"
#include "Engine/StreamableRenderAsset.h"
#include "Engine/Texture.h"
#include "Engine/Texture2D.h"
#include "GameFramework/Actor.h"
#include "GameFramework/Character.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "GameFramework/Controller.h"
#include "GameFramework/ForceFeedbackAttenuation.h"
#include "GameFramework/ForceFeedbackEffect.h"
#include "GameFramework/GameModeBase.h"
#include "GameFramework/GameStateBase.h"
#include "GameFramework/HUD.h"
#include "GameFramework/Info.h"
#include "GameFramework/MovementComponent.h"
#include "GameFramework/NavMovementComponent.h"
#include "GameFramework/Pawn.h"
#include "GameFramework/PawnMovementComponent.h"
#include "GameFramework/PhysicsVolume.h"
#include "GameFramework/PlayerController.h"
#include "GameFramework/PlayerState.h"
#include "GameFramework/SaveGame.h"
#include "GameFramework/TouchInterface.h"
#include "GameFramework/Volume.h"
#include "Kismet/BlueprintFunctionLibrary.h"
#include "Kismet/GameplayStatics.h"
#include "Kismet/KismetMathLibrary.h"
#include "Kismet/KismetStringLibrary.h"
#include "Kismet/KismetSystemLibrary.h"
#include "Materials/MaterialInstance.h"
#include "Materials/MaterialInstanceDynamic.h"
#include "Materials/MaterialInterface.h"
#include "PhysicsEngine/PhysicsAsset.h"
#include "Sound/ReverbEffect.h"
#include "Sound/SoundAttenuation.h"
#include "Sound/SoundBase.h"
#include "Sound/SoundClass.h"
#include "Sound/SoundConcurrency.h"
#include "Sound/SoundMix.h"
#include "UObject/Object.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    Self->TearOff();
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAutoDestroyWhenFinished(Arg0);
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetReplicates(Arg0);
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetReplicateMovement(Arg0);
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    Self->RegisterAsFocalPointInPhysicsReplicationLOD();
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    Self->UnregisterAsFocalPointInPhysicsReplicationLOD();
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->CanTriggerResimulation();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetResimulationThreshold();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetRayTracingGroupId(Arg0);
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetRayTracingGroupId();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    APlayerController* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->EnableInput(Arg0);
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    APlayerController* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->DisableInput(Arg0);
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetInputAxisValue(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetInstigator();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetInstigatorController();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetVelocity();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Rotator")) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    const auto Result = Self->K2_SetActorRotation(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    Self->SetActorScale3D(Arg0);
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetActorScale3D();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetDistanceTo(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetSquaredDistanceTo(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetHorizontalDistanceTo(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetSquaredHorizontalDistanceTo(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetVerticalDistanceTo(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetDotProductTo(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetHorizontalDotProductTo(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    Self->SetActorRelativeScale3D(Arg0);
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetActorRelativeScale3D();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetActorEnableCollision(Arg0);
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    Self->K2_DestroyActor();
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 6) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    FTransform Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[3], Arg2, "Transform")) return;
    UObject* Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    bool Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg4)) return;
    const auto Result = Self->AddComponent(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    UActorComponent* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    FTransform Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[3], Arg2, "Transform")) return;
    Self->FinishAddComponent(Arg0, Arg1, Arg2);
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->DetachRootComponentFromParent(Arg0);
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->ActorHasTag(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetActorTimeDilation();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->AddTickPrerequisiteActor(Arg0);
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    UActorComponent* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->AddTickPrerequisiteComponent(Arg0);
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->RemoveTickPrerequisiteActor(Arg0);
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    UActorComponent* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->RemoveTickPrerequisiteComponent(Arg0);
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetTickableWhenPaused();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetTickableWhenPaused(Arg0);
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetGameTimeSinceCreation();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetLifeSpan(Arg0);
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetLifeSpan();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetActorTickEnabled(Arg0);
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetActorTickInterval(Arg0);
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetActorTickInterval();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetOwner(Arg0);
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->IsOverlappingActor(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    Self->FlushNetDormancy();
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsChildActor();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetParentComponent();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetParentActor();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg1, "Rotator")) return;
    const auto Result = Self->K2_TeleportTo(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetLevel();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetLevelTransform();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetAttachParentActor();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetAttachParentSocketName();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->WasRecentlyRendered(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    Self->ForceNetUpdate();
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->PrestreamTextures(Arg0, Arg1, Arg2);
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 6) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    APawn* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[3], Arg2, "Vector")) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    FName Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg4)) return;
    Self->MakeNoise(Arg0, Arg1, Arg2, Arg3, Arg4);
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetNetUpdateFrequency(Arg0);
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetNetUpdateFrequency();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetMinNetUpdateFrequency(Arg0);
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMinNetUpdateFrequency();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetNetCullDistanceSquared(Arg0);
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AActor*>(B.ReadObject(M, Args[0], AActor::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetNetCullDistanceSquared();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->ComponentHasTag(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->Activate(Arg0);
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    Self->Deactivate();
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetActive(Arg0, Arg1);
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    Self->ToggleActive();
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAutoActivate(Arg0);
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetTickableWhenPaused(Arg0);
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetIsReplicated(Arg0);
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetComponentTickEnabled(Arg0);
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsComponentTickEnabled();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetComponentTickInterval(Arg0);
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetComponentTickIntervalAndCooldown(Arg0);
}
}
void Gather_LhatBundledRuntime_Engine_0(TArray<FLhatNativeType>& Types)
{
    Types.Add({UActorComponent::StaticClass(), "ue.Engine", "ActorComponent"});
    Types.Add({UAnimCompositeBase::StaticClass(), "ue.Engine", "AnimCompositeBase"});
    Types.Add({UAnimInstance::StaticClass(), "ue.Engine", "AnimInstance"});
    Types.Add({UAnimMontage::StaticClass(), "ue.Engine", "AnimMontage"});
    Types.Add({UAnimSequenceBase::StaticClass(), "ue.Engine", "AnimSequenceBase"});
    Types.Add({UAnimationAsset::StaticClass(), "ue.Engine", "AnimationAsset"});
    Types.Add({UAudioComponent::StaticClass(), "ue.Engine", "AudioComponent"});
    Types.Add({UBlueprint::StaticClass(), "ue.Engine", "Blueprint"});
    Types.Add({UBlueprintCore::StaticClass(), "ue.Engine", "BlueprintCore"});
    Types.Add({UBlueprintFunctionLibrary::StaticClass(), "ue.Engine", "BlueprintFunctionLibrary"});
    Types.Add({ABrush::StaticClass(), "ue.Engine", "Brush"});
    Types.Add({ACameraActor::StaticClass(), "ue.Engine", "CameraActor"});
    Types.Add({UCameraShakeSourceComponent::StaticClass(), "ue.Engine", "CameraShakeSourceComponent"});
    Types.Add({ACharacter::StaticClass(), "ue.Engine", "Character"});
    Types.Add({UCharacterMovementComponent::StaticClass(), "ue.Engine", "CharacterMovementComponent"});
    Types.Add({UChildActorComponent::StaticClass(), "ue.Engine", "ChildActorComponent"});
    Types.Add({AController::StaticClass(), "ue.Engine", "Controller"});
    Types.Add({UDecalComponent::StaticClass(), "ue.Engine", "DecalComponent"});
    Types.Add({UForceFeedbackAttenuation::StaticClass(), "ue.Engine", "ForceFeedbackAttenuation"});
    Types.Add({UForceFeedbackComponent::StaticClass(), "ue.Engine", "ForceFeedbackComponent"});
    Types.Add({UForceFeedbackEffect::StaticClass(), "ue.Engine", "ForceFeedbackEffect"});
    Types.Add({UGameInstance::StaticClass(), "ue.Engine", "GameInstance"});
    Types.Add({AGameModeBase::StaticClass(), "ue.Engine", "GameModeBase"});
    Types.Add({AGameStateBase::StaticClass(), "ue.Engine", "GameStateBase"});
    Types.Add({UGameplayStatics::StaticClass(), "ue.Engine", "GameplayStatics"});
    Types.Add({AHUD::StaticClass(), "ue.Engine", "HUD"});
    Types.Add({AInfo::StaticClass(), "ue.Engine", "Info"});
    Types.Add({UInitialActiveSoundParams::StaticClass(), "ue.Engine", "InitialActiveSoundParams"});
    Types.Add({UKismetMathLibrary::StaticClass(), "ue.Engine", "KismetMathLibrary"});
    Types.Add({UKismetStringLibrary::StaticClass(), "ue.Engine", "KismetStringLibrary"});
    Types.Add({UKismetSystemLibrary::StaticClass(), "ue.Engine", "KismetSystemLibrary"});
    Types.Add({ULevel::StaticClass(), "ue.Engine", "Level"});
    Types.Add({ULevelStreaming::StaticClass(), "ue.Engine", "LevelStreaming"});
    Types.Add({UMaterialInstance::StaticClass(), "ue.Engine", "MaterialInstance"});
    Types.Add({UMaterialInstanceDynamic::StaticClass(), "ue.Engine", "MaterialInstanceDynamic"});
    Types.Add({UMaterialInterface::StaticClass(), "ue.Engine", "MaterialInterface"});
    Types.Add({UMeshComponent::StaticClass(), "ue.Engine", "MeshComponent"});
    Types.Add({UMovementComponent::StaticClass(), "ue.Engine", "MovementComponent"});
    Types.Add({UNavMovementComponent::StaticClass(), "ue.Engine", "NavMovementComponent"});
    Types.Add({APawn::StaticClass(), "ue.Engine", "Pawn"});
    Types.Add({UPawnMovementComponent::StaticClass(), "ue.Engine", "PawnMovementComponent"});
    Types.Add({UPhysicsAsset::StaticClass(), "ue.Engine", "PhysicsAsset"});
    Types.Add({APhysicsVolume::StaticClass(), "ue.Engine", "PhysicsVolume"});
    Types.Add({APlayerCameraManager::StaticClass(), "ue.Engine", "PlayerCameraManager"});
    Types.Add({APlayerController::StaticClass(), "ue.Engine", "PlayerController"});
    Types.Add({APlayerState::StaticClass(), "ue.Engine", "PlayerState"});
    Types.Add({UPrimitiveComponent::StaticClass(), "ue.Engine", "PrimitiveComponent"});
    Types.Add({UReverbEffect::StaticClass(), "ue.Engine", "ReverbEffect"});
    Types.Add({USaveGame::StaticClass(), "ue.Engine", "SaveGame"});
    Types.Add({USceneComponent::StaticClass(), "ue.Engine", "SceneComponent"});
    Types.Add({USkeletalMesh::StaticClass(), "ue.Engine", "SkeletalMesh"});
    Types.Add({USkeletalMeshComponent::StaticClass(), "ue.Engine", "SkeletalMeshComponent"});
    Types.Add({USkinnedAsset::StaticClass(), "ue.Engine", "SkinnedAsset"});
    Types.Add({USkinnedMeshComponent::StaticClass(), "ue.Engine", "SkinnedMeshComponent"});
    Types.Add({USoundAttenuation::StaticClass(), "ue.Engine", "SoundAttenuation"});
    Types.Add({USoundBase::StaticClass(), "ue.Engine", "SoundBase"});
    Types.Add({USoundClass::StaticClass(), "ue.Engine", "SoundClass"});
    Types.Add({USoundConcurrency::StaticClass(), "ue.Engine", "SoundConcurrency"});
    Types.Add({USoundMix::StaticClass(), "ue.Engine", "SoundMix"});
    Types.Add({UStaticMesh::StaticClass(), "ue.Engine", "StaticMesh"});
    Types.Add({UStaticMeshComponent::StaticClass(), "ue.Engine", "StaticMeshComponent"});
    Types.Add({UStreamableRenderAsset::StaticClass(), "ue.Engine", "StreamableRenderAsset"});
    Types.Add({UTexture::StaticClass(), "ue.Engine", "Texture"});
    Types.Add({UTexture2D::StaticClass(), "ue.Engine", "Texture2D"});
    Types.Add({UTouchInterface::StaticClass(), "ue.Engine", "TouchInterface"});
    Types.Add({AVolume::StaticClass(), "ue.Engine", "Volume"});
}
bool Register_LhatBundledRuntime_Engine_0(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue", "Actor", "TearOff", "p^self^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetAutoDestroyWhenFinished", "p^self^, bool^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetReplicates", "p^self^, bool^;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetReplicateMovement", "p^self^, bool^;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "RegisterAsFocalPointInPhysicsReplicationLOD", "p^self^;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "UnregisterAsFocalPointInPhysicsReplicationLOD", "p^self^;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "CanTriggerResimulation", "f^self^ -> bool^;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetResimulationThreshold", "f^self^ -> number^;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetRayTracingGroupId", "p^self^, number^;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetRayTracingGroupId", "f^self^ -> number^;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "EnableInput", "p^self^, ue.Engine.PlayerController|nil^;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "DisableInput", "p^self^, ue.Engine.PlayerController|nil^;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetInputAxisValue", "f^self^, string^ -> number^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetInstigator", "f^self^ -> ue.Engine.Pawn|nil^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetInstigatorController", "f^self^ -> ue.Engine.Controller|nil^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetVelocity", "f^self^ -> ue.Vector;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "K2_SetActorRotation", "p^self^, ue.Rotator, bool^ -> bool^;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetActorScale3D", "p^self^, ue.Vector;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetActorScale3D", "f^self^ -> ue.Vector;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetDistanceTo", "f^self^, ue.Actor|nil^ -> number^;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetSquaredDistanceTo", "f^self^, ue.Actor|nil^ -> number^;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetHorizontalDistanceTo", "f^self^, ue.Actor|nil^ -> number^;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetSquaredHorizontalDistanceTo", "f^self^, ue.Actor|nil^ -> number^;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetVerticalDistanceTo", "f^self^, ue.Actor|nil^ -> number^;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetDotProductTo", "f^self^, ue.Actor|nil^ -> number^;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetHorizontalDotProductTo", "f^self^, ue.Actor|nil^ -> number^;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetActorRelativeScale3D", "p^self^, ue.Vector;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetActorRelativeScale3D", "f^self^ -> ue.Vector;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetActorEnableCollision", "p^self^, bool^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "K2_DestroyActor", "p^self^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "AddComponent", "p^self^, string^, bool^, ue.Transform, ue.Object|nil^, bool^ -> ue.Engine.ActorComponent|nil^;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "FinishAddComponent", "p^self^, ue.Engine.ActorComponent|nil^, bool^, ue.Transform;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "DetachRootComponentFromParent", "p^self^, bool^;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "ActorHasTag", "f^self^, string^ -> bool^;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetActorTimeDilation", "f^self^ -> number^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "AddTickPrerequisiteActor", "p^self^, ue.Actor|nil^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "AddTickPrerequisiteComponent", "p^self^, ue.Engine.ActorComponent|nil^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "RemoveTickPrerequisiteActor", "p^self^, ue.Actor|nil^;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "RemoveTickPrerequisiteComponent", "p^self^, ue.Engine.ActorComponent|nil^;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetTickableWhenPaused", "p^self^ -> bool^;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetTickableWhenPaused", "p^self^, bool^;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetGameTimeSinceCreation", "f^self^ -> number^;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetLifeSpan", "p^self^, number^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetLifeSpan", "f^self^ -> number^;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetActorTickEnabled", "p^self^, bool^;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetActorTickInterval", "p^self^, number^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetActorTickInterval", "f^self^ -> number^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetOwner", "p^self^, ue.Actor|nil^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "IsOverlappingActor", "f^self^, ue.Actor|nil^ -> bool^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "FlushNetDormancy", "p^self^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "IsChildActor", "f^self^ -> bool^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetParentComponent", "f^self^ -> ue.Engine.ChildActorComponent|nil^;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetParentActor", "f^self^ -> ue.Actor|nil^;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "K2_TeleportTo", "p^self^, ue.Vector, ue.Rotator -> bool^;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetLevel", "f^self^ -> ue.Engine.Level|nil^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetLevelTransform", "f^self^ -> ue.Transform;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetAttachParentActor", "f^self^ -> ue.Actor|nil^;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetAttachParentSocketName", "f^self^ -> string^;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "WasRecentlyRendered", "f^self^, number^ -> bool^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "ForceNetUpdate", "p^self^;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "PrestreamTextures", "p^self^, number^, bool^, number^;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "MakeNoise", "p^self^, number^, ue.Engine.Pawn|nil^, ue.Vector, number^, string^;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetNetUpdateFrequency", "p^self^, number^;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetNetUpdateFrequency", "f^self^ -> number^;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetMinNetUpdateFrequency", "p^self^, number^;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetMinNetUpdateFrequency", "f^self^ -> number^;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "SetNetCullDistanceSquared", "p^self^, number^;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue", "Actor", "GetNetCullDistanceSquared", "f^self^ -> number^;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "ComponentHasTag", "f^self^, string^ -> bool^;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "Activate", "p^self^, bool^;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "Deactivate", "p^self^;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "SetActive", "p^self^, bool^, bool^;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "ToggleActive", "p^self^;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "SetAutoActivate", "p^self^, bool^;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "SetTickableWhenPaused", "p^self^, bool^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "SetIsReplicated", "p^self^, bool^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "SetComponentTickEnabled", "p^self^, bool^;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "IsComponentTickEnabled", "f^self^ -> bool^;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "SetComponentTickInterval", "p^self^, number^;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "SetComponentTickIntervalAndCooldown", "p^self^, number^;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
