// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Animation/AnimInstance.h"
#include "Animation/AnimationAsset.h"
#include "ClothingSimulationInteractor.h"
#include "Components/SceneComponent.h"
#include "Components/SkeletalMeshComponent.h"
#include "Engine/SkeletalMesh.h"
#include "GameFramework/PhysicsVolume.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetComponentVelocity();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsVisible();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetPhysicsVolume();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USceneComponent*>(B.ReadObject(M, Args[0], USceneComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetAbsolute(Arg0, Arg1, Arg2);
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetSkeletalMeshAsset();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ToggleDisablePostProcessBlueprint();
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetDisablePostProcessBlueprint();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetDisablePostProcessBlueprint(Arg0);
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetEnableAnimation(Arg0);
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetAnimInstance();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetPostProcessInstance();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetLinkedAnimGraphInstanceByTag(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetLinkedAnimLayerInstanceByGroup(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->HasValidAnimationInstance();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    UAnimationAsset* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->PlayAnimation(Arg0, Arg1);
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    UAnimationAsset* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAnimation(Arg0);
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->Play(Arg0);
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->Stop();
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsPlaying();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetPosition(Arg0, Arg1);
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetPosition();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetPlayRate(Arg0);
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetPlayRate();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 6) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    UAnimationAsset* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    float Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[5], Arg4)) return;
    Self->OverrideAnimationData(Arg0, Arg1, Arg2, Arg3, Arg4);
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetMorphTarget(Arg0, Arg1, Arg2);
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ClearMorphTargets();
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetMorphTarget(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAllowClothActors(Arg0);
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetClothMaxDistanceScale();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetClothMaxDistanceScale(Arg0);
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ForceClothNextUpdateTeleport();
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ForceClothNextUpdateTeleportAndReset();
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->SuspendClothingSimulation();
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ResumeClothingSimulation();
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsClothingSimulationSuspended();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ResetClothTeleportMode();
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->BindClothToLeaderPoseComponent();
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->UnbindClothFromLeaderPoseComponent(Arg0);
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetAllowRigidBodyAnimNode(Arg0, Arg1);
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetAllowAnimDynamicsAnimNode(Arg0, Arg1);
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ReinitializeAnimNodes();
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetUpdateAnimationInEditor(Arg0);
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetUpdateClothInEditor(Arg0);
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetDisableAnimCurves(Arg0);
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAllowAnimCurveEvaluation(Arg0);
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->AllowAnimCurveEvaluation(Arg0, Arg1);
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ResetAllowedAnimCurveEvaluation();
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetTeleportRotationThreshold();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetTeleportRotationThreshold(Arg0);
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetTeleportDistanceThreshold();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetTeleportDistanceThreshold(Arg0);
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetClothingSimulationInteractor(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetBodyNotifyRigidBodyCollision(Arg0, Arg1);
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetNotifyRigidBodyCollisionBelow(Arg0, Arg1, Arg2);
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetEnablePerPolyCollision(Arg0);
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetEnableBodyGravity(Arg0, Arg1);
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->IsBodyGravityEnabled(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetEnableGravityOnAllBodiesBelow(Arg0, Arg1, Arg2);
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    const auto Result = Self->GetBoneMass(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetSkeletalCenterOfMass();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    Self->AddForceToAllBodiesBelow(Arg0, Arg1, Arg2, Arg3);
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    Self->AddImpulseToAllBodiesBelow(Arg0, Arg1, Arg2, Arg3);
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->TermBodiesBelow(Arg0);
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAllBodiesSimulatePhysics(Arg0);
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetPhysicsBlendWeight(Arg0);
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetEnablePhysicsBlending(Arg0);
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetAllBodiesBelowPhysicsDisabled(Arg0, Arg1, Arg2);
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg1, "Vector")) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetAllBodiesBelowLinearVelocity(Arg0, Arg1, Arg2);
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    const auto Result = Self->GetBoneLinearVelocity(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetAllBodiesBelowSimulatePhysics(Arg0, Arg1, Arg2);
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetBodySimulatePhysics(Arg0, Arg1);
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    Self->ResetAllBodiesSimulatePhysics();
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetAllBodiesPhysicsBlendWeight(Arg0, Arg1);
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    Self->SetAllBodiesBelowPhysicsBlendWeight(Arg0, Arg1, Arg2, Arg3);
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->AccumulateAllBodiesBelowPhysicsBlendWeight(Arg0, Arg1, Arg2);
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetAllMotorsAngularPositionDrive(Arg0, Arg1, Arg2);
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetAllMotorsAngularVelocityDrive(Arg0, Arg1, Arg2);
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    Self->SetAllMotorsAngularDriveParams(Arg0, Arg1, Arg2, Arg3);
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->SetConstraintProfile(Arg0, Arg1, Arg2);
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<USkeletalMeshComponent*>(B.ReadObject(M, Args[0], USkeletalMeshComponent::StaticClass()));
    if (!Self) return;
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->SetConstraintProfileForAll(Arg0, Arg1);
}
}
void Gather_LhatBundledRuntime_Engine_13(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledRuntime_Engine_13(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetComponentVelocity", "f^self^ -> ue.Vector;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "IsVisible", "f^self^ -> bool^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "GetPhysicsVolume", "f^self^ -> ue.Engine.PhysicsVolume|nil^;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SceneComponent", "SetAbsolute", "p^self^, bool^, bool^, bool^;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetSkeletalMeshAsset", "f^self^ -> ue.Engine.SkeletalMesh|nil^;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ToggleDisablePostProcessBlueprint", "p^self^;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetDisablePostProcessBlueprint", "f^self^ -> bool^;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetDisablePostProcessBlueprint", "p^self^, bool^;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetEnableAnimation", "p^self^, bool^;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetAnimInstance", "f^self^ -> ue.Engine.AnimInstance|nil^;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetPostProcessInstance", "f^self^ -> ue.Engine.AnimInstance|nil^;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetLinkedAnimGraphInstanceByTag", "f^self^, string^ -> ue.Engine.AnimInstance|nil^;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetLinkedAnimLayerInstanceByGroup", "f^self^, string^ -> ue.Engine.AnimInstance|nil^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "HasValidAnimationInstance", "f^self^ -> bool^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "PlayAnimation", "p^self^, ue.Engine.AnimationAsset|nil^, bool^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAnimation", "p^self^, ue.Engine.AnimationAsset|nil^;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "Play", "p^self^, bool^;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "Stop", "p^self^;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "IsPlaying", "f^self^ -> bool^;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetPosition", "p^self^, number^, bool^;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetPosition", "f^self^ -> number^;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetPlayRate", "p^self^, number^;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetPlayRate", "f^self^ -> number^;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "OverrideAnimationData", "p^self^, ue.Engine.AnimationAsset|nil^, bool^, bool^, number^, number^;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetMorphTarget", "p^self^, string^, number^, bool^;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ClearMorphTargets", "p^self^;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetMorphTarget", "f^self^, string^ -> number^;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllowClothActors", "p^self^, bool^;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetClothMaxDistanceScale", "f^self^ -> number^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetClothMaxDistanceScale", "p^self^, number^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ForceClothNextUpdateTeleport", "p^self^;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ForceClothNextUpdateTeleportAndReset", "p^self^;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SuspendClothingSimulation", "p^self^;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ResumeClothingSimulation", "p^self^;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "IsClothingSimulationSuspended", "f^self^ -> bool^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ResetClothTeleportMode", "p^self^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "BindClothToLeaderPoseComponent", "p^self^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "UnbindClothFromLeaderPoseComponent", "p^self^, bool^;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllowRigidBodyAnimNode", "p^self^, bool^, bool^;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllowAnimDynamicsAnimNode", "p^self^, bool^, bool^;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ReinitializeAnimNodes", "p^self^;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetUpdateAnimationInEditor", "p^self^, bool^;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetUpdateClothInEditor", "p^self^, bool^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetDisableAnimCurves", "p^self^, bool^;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllowAnimCurveEvaluation", "p^self^, bool^;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "AllowAnimCurveEvaluation", "p^self^, string^, bool^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ResetAllowedAnimCurveEvaluation", "p^self^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetTeleportRotationThreshold", "f^self^ -> number^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetTeleportRotationThreshold", "p^self^, number^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetTeleportDistanceThreshold", "f^self^ -> number^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetTeleportDistanceThreshold", "p^self^, number^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetClothingSimulationInteractor", "f^self^, string^ -> ue.ClothingSystemRuntimeInterface.ClothingSimulationInteractor|nil^;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetBodyNotifyRigidBodyCollision", "p^self^, bool^, string^;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetNotifyRigidBodyCollisionBelow", "p^self^, bool^, string^, bool^;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetEnablePerPolyCollision", "p^self^, bool^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetEnableBodyGravity", "p^self^, bool^, string^;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "IsBodyGravityEnabled", "p^self^, string^ -> bool^;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetEnableGravityOnAllBodiesBelow", "p^self^, bool^, string^, bool^;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetBoneMass", "f^self^, string^, bool^ -> number^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetSkeletalCenterOfMass", "f^self^ -> ue.Vector;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "AddForceToAllBodiesBelow", "p^self^, ue.Vector, string^, bool^, bool^;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "AddImpulseToAllBodiesBelow", "p^self^, ue.Vector, string^, bool^, bool^;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "TermBodiesBelow", "p^self^, string^;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllBodiesSimulatePhysics", "p^self^, bool^;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetPhysicsBlendWeight", "p^self^, number^;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetEnablePhysicsBlending", "p^self^, bool^;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllBodiesBelowPhysicsDisabled", "p^self^, string^, bool^, bool^;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllBodiesBelowLinearVelocity", "p^self^, string^, ue.Vector, bool^;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "GetBoneLinearVelocity", "p^self^, string^ -> ue.Vector;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllBodiesBelowSimulatePhysics", "p^self^, string^, bool^, bool^;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetBodySimulatePhysics", "p^self^, string^, bool^;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "ResetAllBodiesSimulatePhysics", "p^self^;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllBodiesPhysicsBlendWeight", "p^self^, number^, bool^;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllBodiesBelowPhysicsBlendWeight", "p^self^, string^, number^, bool^, bool^;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "AccumulateAllBodiesBelowPhysicsBlendWeight", "p^self^, string^, number^, bool^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllMotorsAngularPositionDrive", "p^self^, bool^, bool^, bool^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllMotorsAngularVelocityDrive", "p^self^, bool^, bool^, bool^;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetAllMotorsAngularDriveParams", "p^self^, number^, number^, number^, bool^;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetConstraintProfile", "p^self^, string^, string^, bool^;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "SkeletalMeshComponent", "SetConstraintProfileForAll", "p^self^, string^, bool^;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
