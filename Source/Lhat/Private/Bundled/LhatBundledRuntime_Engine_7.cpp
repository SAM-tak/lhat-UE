// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Kismet/KismetMathLibrary.h"
#include "UObject/Object.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FQuat Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Quat")) return;
    const auto Result = UKismetMathLibrary::Quat_Size(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FQuat Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Quat")) return;
    const auto Result = UKismetMathLibrary::Quat_SizeSquared(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FQuat Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Quat")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::Quat_RotateVector(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FQuat Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Quat")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::Quat_UnrotateVector(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FQuat Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Quat")) return;
    FQuat Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Quat")) return;
    double Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::Quat_Slerp(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Quat")) *AnswerCount = 1;
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::Quat_FindBetweenVectors(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Quat")) *AnswerCount = 1;
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    const auto Result = UKismetMathLibrary::Quat_FindBetweenNormals(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Quat")) *AnswerCount = 1;
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_White();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_Gray();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_Black();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_Red();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_Green();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_Blue();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_Yellow();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 0) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    const auto Result = UKismetMathLibrary::LinearColor_Transparent();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::MakeColor(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_DoubleToLinearColor(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::HSVToRGB(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::HSVToRGBLinear(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::RGBLinearToHSV(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::Conv_LinearColorToVector(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::LinearColor_ToRGBE(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Color")) *AnswerCount = 1;
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Conv_LinearColorToColor(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Color")) *AnswerCount = 1;
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::LinearColor_Quantize(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Color")) *AnswerCount = 1;
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::LinearColor_QuantizeRound(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Color")) *AnswerCount = 1;
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::LinearColor_Desaturated(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::LinearColor_Distance(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::LinearColor_ToNewOpacity(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::LinearColor_GetLuminance(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::LinearColor_GetMax(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::LinearColor_GetMin(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UKismetMathLibrary::CInterpTo(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::LinearColorLerp(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::LinearColorLerpUsingHSV(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::LinearColor_IsNearEqual(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::EqualEqual_LinearColorLinearColor(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::NotEqual_LinearColorLinearColor(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::Add_LinearColorLinearColor(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::Subtract_LinearColorLinearColor(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::Multiply_LinearColorLinearColor(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Multiply_LinearColorFloat(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::Divide_LinearColorLinearColor(Arg0, Arg1);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    const auto Result = UKismetMathLibrary::ToHex_LinearColor(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::DaysInMonth(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::DaysInYear(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::IsLeapYear(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_ByteToDouble(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_IntToDouble(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_IntToInt64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_IntToByte(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_Int64ToInt(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_DoubleToFloat(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_FloatToDouble(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_Int64ToByte(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_DoubleToInt64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_Int64ToDouble(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_IntToVector(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_IntToBool(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_BoolToInt(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_BoolToDouble(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_BoolToByte(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_ByteToInt(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_ByteToInt64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Color")) return;
    const auto Result = UKismetMathLibrary::Conv_ColorToLinearColor(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_DoubleToVector(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Conv_DoubleToVector2D(Arg0);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector2D")) *AnswerCount = 1;
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FString Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FString Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectString(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectName(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectInt(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectFloat(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectVector(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Rotator")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectRotator(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FLinearColor Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "LinearColor")) return;
    FLinearColor Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "LinearColor")) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectColor(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "LinearColor")) *AnswerCount = 1;
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FTransform Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Transform")) return;
    FTransform Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Transform")) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectTransform(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UObject* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::SelectObject(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UObject* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::EqualEqual_ObjectObject(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UObject* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::NotEqual_ObjectObject(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::EqualEqual_NameName(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FName Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FName Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::NotEqual_NameName(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[0], Arg0, "Vector")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Rotator")) return;
    FVector Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Vector")) return;
    const auto Result = UKismetMathLibrary::MakeTransform(Arg0, Arg1, Arg2);
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Transform")) *AnswerCount = 1;
}
}
void Gather_LhatBundledRuntime_Engine_7(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledRuntime_Engine_7(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Quat_Size", "f^ue.Quat -> number^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Quat_SizeSquared", "f^ue.Quat -> number^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Quat_RotateVector", "f^ue.Quat, ue.Vector -> ue.Vector;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Quat_UnrotateVector", "f^ue.Quat, ue.Vector -> ue.Vector;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Quat_Slerp", "f^ue.Quat, ue.Quat, number^ -> ue.Quat;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Quat_FindBetweenVectors", "f^ue.Vector, ue.Vector -> ue.Quat;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Quat_FindBetweenNormals", "f^ue.Vector, ue.Vector -> ue.Quat;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_White", "f^ -> ue.LinearColor;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Gray", "f^ -> ue.LinearColor;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Black", "f^ -> ue.LinearColor;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Red", "f^ -> ue.LinearColor;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Green", "f^ -> ue.LinearColor;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Blue", "f^ -> ue.LinearColor;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Yellow", "f^ -> ue.LinearColor;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Transparent", "f^ -> ue.LinearColor;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "MakeColor", "f^number^, number^, number^, number^ -> ue.LinearColor;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_DoubleToLinearColor", "f^number^ -> ue.LinearColor;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "HSVToRGB", "f^number^, number^, number^, number^ -> ue.LinearColor;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "HSVToRGBLinear", "f^ue.LinearColor -> ue.LinearColor;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RGBLinearToHSV", "f^ue.LinearColor -> ue.LinearColor;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_LinearColorToVector", "f^ue.LinearColor -> ue.Vector;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_ToRGBE", "f^ue.LinearColor -> ue.Color;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_LinearColorToColor", "f^ue.LinearColor, bool^ -> ue.Color;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Quantize", "f^ue.LinearColor -> ue.Color;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_QuantizeRound", "f^ue.LinearColor -> ue.Color;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Desaturated", "f^ue.LinearColor, number^ -> ue.LinearColor;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_Distance", "f^ue.LinearColor, ue.LinearColor -> number^;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_ToNewOpacity", "f^ue.LinearColor, number^ -> ue.LinearColor;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_GetLuminance", "f^ue.LinearColor -> number^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_GetMax", "f^ue.LinearColor -> number^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_GetMin", "f^ue.LinearColor -> number^;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "CInterpTo", "f^ue.LinearColor, ue.LinearColor, number^, number^ -> ue.LinearColor;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColorLerp", "f^ue.LinearColor, ue.LinearColor, number^ -> ue.LinearColor;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColorLerpUsingHSV", "f^ue.LinearColor, ue.LinearColor, number^ -> ue.LinearColor;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LinearColor_IsNearEqual", "f^ue.LinearColor, ue.LinearColor, number^ -> bool^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_LinearColorLinearColor", "f^ue.LinearColor, ue.LinearColor -> bool^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_LinearColorLinearColor", "f^ue.LinearColor, ue.LinearColor -> bool^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Add_LinearColorLinearColor", "f^ue.LinearColor, ue.LinearColor -> ue.LinearColor;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Subtract_LinearColorLinearColor", "f^ue.LinearColor, ue.LinearColor -> ue.LinearColor;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Multiply_LinearColorLinearColor", "f^ue.LinearColor, ue.LinearColor -> ue.LinearColor;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Multiply_LinearColorFloat", "f^ue.LinearColor, number^ -> ue.LinearColor;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Divide_LinearColorLinearColor", "f^ue.LinearColor, ue.LinearColor -> ue.LinearColor;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "ToHex_LinearColor", "f^ue.LinearColor -> string^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "DaysInMonth", "f^number^, number^ -> number^;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "DaysInYear", "f^number^ -> number^;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "IsLeapYear", "f^number^ -> bool^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_ByteToDouble", "f^number^ -> number^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_IntToDouble", "f^number^ -> number^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_IntToInt64", "f^number^ -> number^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_IntToByte", "f^number^ -> number^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_Int64ToInt", "f^number^ -> number^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_DoubleToFloat", "f^number^ -> number^;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_FloatToDouble", "f^number^ -> number^;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_Int64ToByte", "f^number^ -> number^;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_DoubleToInt64", "f^number^ -> number^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_Int64ToDouble", "f^number^ -> number^;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_IntToVector", "f^number^ -> ue.Vector;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_IntToBool", "f^number^ -> bool^;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_BoolToInt", "f^bool^ -> number^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_BoolToDouble", "f^bool^ -> number^;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_BoolToByte", "f^bool^ -> number^;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_ByteToInt", "f^number^ -> number^;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_ByteToInt64", "f^number^ -> number^;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_ColorToLinearColor", "f^ue.Color -> ue.LinearColor;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_DoubleToVector", "f^number^ -> ue.Vector;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Conv_DoubleToVector2D", "f^number^ -> ue.Vector2D;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectString", "f^string^, string^, bool^ -> string^;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectName", "f^string^, string^, bool^ -> string^;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectInt", "f^number^, number^, bool^ -> number^;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectFloat", "f^number^, number^, bool^ -> number^;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectVector", "f^ue.Vector, ue.Vector, bool^ -> ue.Vector;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectRotator", "f^ue.Rotator, ue.Rotator, bool^ -> ue.Rotator;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectColor", "f^ue.LinearColor, ue.LinearColor, bool^ -> ue.LinearColor;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectTransform", "f^ue.Transform, ue.Transform, bool^ -> ue.Transform;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SelectObject", "f^ue.Object|nil^, ue.Object|nil^, bool^ -> ue.Object|nil^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_ObjectObject", "f^ue.Object|nil^, ue.Object|nil^ -> bool^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_ObjectObject", "f^ue.Object|nil^, ue.Object|nil^ -> bool^;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_NameName", "f^string^, string^ -> bool^;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_NameName", "f^string^, string^ -> bool^;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "MakeTransform", "f^ue.Vector, ue.Rotator, ue.Vector -> ue.Transform;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
