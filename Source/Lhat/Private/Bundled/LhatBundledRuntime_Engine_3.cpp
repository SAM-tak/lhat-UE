// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Kismet/KismetMathLibrary.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Percent_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Add_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Subtract_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::BMin(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::BMax(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Less_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Greater_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::LessEqual_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::GreaterEqual_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::EqualEqual_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    uint8 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    uint8 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::NotEqual_ByteByte(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Multiply_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Divide_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Percent_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Add_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Subtract_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Less_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Greater_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::LessEqual_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::GreaterEqual_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::EqualEqual_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::NotEqual_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    bool Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetMathLibrary::InRange_IntInt(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::And_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Xor_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Or_IntInt(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Not_Int(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::SignOfInteger(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::RandomInteger(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::RandomIntegerInRange(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Min(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Max(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::Clamp(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int32 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::Wrap(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Abs_Int(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Multiply_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Divide_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Percent_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Add_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Subtract_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Less_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Greater_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::LessEqual_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::GreaterEqual_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::EqualEqual_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::NotEqual_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int64 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    bool Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetMathLibrary::InRange_Int64Int64(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::And_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Xor_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Or_Int64Int64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Not_Int64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::SignOfInteger64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::RandomInteger64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::RandomInteger64InRange(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::MinInt64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::MaxInt64(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int64 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    int64 Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::ClampInt64(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int64 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Abs_Int64(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::MultiplyMultiply_FloatFloat(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Multiply_IntFloat(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Fraction(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Add_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Subtract_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Multiply_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Divide_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Less_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Greater_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::LessEqual_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::GreaterEqual_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::EqualEqual_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    double Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UKismetMathLibrary::NearlyEqual_FloatFloat(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::NotEqual_DoubleDouble(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    double Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    bool Arg4{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg4)) return;
    const auto Result = UKismetMathLibrary::InRange_FloatFloat(Arg0, Arg1, Arg2, Arg3, Arg4);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::Hypotenuse(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    double Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UKismetMathLibrary::GridSnap_Float(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Abs(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Sin(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Asin(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Cos(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    double Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UKismetMathLibrary::Acos(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
}
void Gather_LhatBundledRuntime_Engine_3(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledRuntime_Engine_3(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Percent_ByteByte", "f^number^, number^ -> number^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Add_ByteByte", "f^number^, number^ -> number^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Subtract_ByteByte", "f^number^, number^ -> number^;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "BMin", "f^number^, number^ -> number^;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "BMax", "f^number^, number^ -> number^;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Less_ByteByte", "f^number^, number^ -> bool^;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Greater_ByteByte", "f^number^, number^ -> bool^;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LessEqual_ByteByte", "f^number^, number^ -> bool^;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "GreaterEqual_ByteByte", "f^number^, number^ -> bool^;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_ByteByte", "f^number^, number^ -> bool^;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_ByteByte", "f^number^, number^ -> bool^;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Multiply_IntInt", "f^number^, number^ -> number^;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Divide_IntInt", "f^number^, number^ -> number^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Percent_IntInt", "f^number^, number^ -> number^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Add_IntInt", "f^number^, number^ -> number^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Subtract_IntInt", "f^number^, number^ -> number^;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Less_IntInt", "f^number^, number^ -> bool^;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Greater_IntInt", "f^number^, number^ -> bool^;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LessEqual_IntInt", "f^number^, number^ -> bool^;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "GreaterEqual_IntInt", "f^number^, number^ -> bool^;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_IntInt", "f^number^, number^ -> bool^;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_IntInt", "f^number^, number^ -> bool^;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "InRange_IntInt", "f^number^, number^, number^, bool^, bool^ -> bool^;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "And_IntInt", "f^number^, number^ -> number^;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Xor_IntInt", "f^number^, number^ -> number^;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Or_IntInt", "f^number^, number^ -> number^;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Not_Int", "f^number^ -> number^;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SignOfInteger", "f^number^ -> number^;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RandomInteger", "f^number^ -> number^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RandomIntegerInRange", "f^number^, number^ -> number^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Min", "f^number^, number^ -> number^;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Max", "f^number^, number^ -> number^;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Clamp", "f^number^, number^, number^ -> number^;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Wrap", "f^number^, number^, number^ -> number^;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Abs_Int", "f^number^ -> number^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Multiply_Int64Int64", "f^number^, number^ -> number^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Divide_Int64Int64", "f^number^, number^ -> number^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Percent_Int64Int64", "f^number^, number^ -> number^;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Add_Int64Int64", "f^number^, number^ -> number^;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Subtract_Int64Int64", "f^number^, number^ -> number^;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Less_Int64Int64", "f^number^, number^ -> bool^;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Greater_Int64Int64", "f^number^, number^ -> bool^;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LessEqual_Int64Int64", "f^number^, number^ -> bool^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "GreaterEqual_Int64Int64", "f^number^, number^ -> bool^;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_Int64Int64", "f^number^, number^ -> bool^;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_Int64Int64", "f^number^, number^ -> bool^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "InRange_Int64Int64", "f^number^, number^, number^, bool^, bool^ -> bool^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "And_Int64Int64", "f^number^, number^ -> number^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Xor_Int64Int64", "f^number^, number^ -> number^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Or_Int64Int64", "f^number^, number^ -> number^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Not_Int64", "f^number^ -> number^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "SignOfInteger64", "f^number^ -> number^;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RandomInteger64", "f^number^ -> number^;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "RandomInteger64InRange", "f^number^, number^ -> number^;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "MinInt64", "f^number^, number^ -> number^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "MaxInt64", "f^number^, number^ -> number^;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "ClampInt64", "f^number^, number^, number^ -> number^;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Abs_Int64", "f^number^ -> number^;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "MultiplyMultiply_FloatFloat", "f^number^, number^ -> number^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Multiply_IntFloat", "f^number^, number^ -> number^;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Fraction", "f^number^ -> number^;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Add_DoubleDouble", "f^number^, number^ -> number^;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Subtract_DoubleDouble", "f^number^, number^ -> number^;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Multiply_DoubleDouble", "f^number^, number^ -> number^;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Divide_DoubleDouble", "f^number^, number^ -> number^;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Less_DoubleDouble", "f^number^, number^ -> bool^;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Greater_DoubleDouble", "f^number^, number^ -> bool^;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "LessEqual_DoubleDouble", "f^number^, number^ -> bool^;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "GreaterEqual_DoubleDouble", "f^number^, number^ -> bool^;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "EqualEqual_DoubleDouble", "f^number^, number^ -> bool^;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NearlyEqual_FloatFloat", "f^number^, number^, number^ -> bool^;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "NotEqual_DoubleDouble", "f^number^, number^ -> bool^;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "InRange_FloatFloat", "f^number^, number^, number^, bool^, bool^ -> bool^;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Hypotenuse", "f^number^, number^ -> number^;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "GridSnap_Float", "f^number^, number^ -> number^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Abs", "f^number^ -> number^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Sin", "f^number^ -> number^;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Asin", "f^number^ -> number^;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Cos", "f^number^ -> number^;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "KismetMathLibrary", "Acos", "f^number^ -> number^;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
