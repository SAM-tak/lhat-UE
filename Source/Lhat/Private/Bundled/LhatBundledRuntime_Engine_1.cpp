// Generated by GenerateNativeBindings.py; do not edit.
#include "LhatNativeBindings.h"
PRAGMA_DISABLE_DEPRECATION_WARNINGS
#include "Animation/AnimMontage.h"
#include "Camera/PlayerCameraManager.h"
#include "Components/ActorComponent.h"
#include "Components/PrimitiveComponent.h"
#include "Engine/Blueprint.h"
#include "Engine/GameInstance.h"
#include "GameFramework/Actor.h"
#include "GameFramework/Character.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "GameFramework/Controller.h"
#include "GameFramework/Pawn.h"
#include "GameFramework/PlayerController.h"
#include "GameFramework/PlayerState.h"
#include "Kismet/GameplayStatics.h"
#include "UObject/Object.h"
namespace {
static void Call0(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetComponentTickInterval();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call1(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->K2_DestroyComponent(Arg0);
}
static void Call2(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->AddTickPrerequisiteActor(Arg0);
}
static void Call3(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    UActorComponent* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->AddTickPrerequisiteComponent(Arg0);
}
static void Call4(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->RemoveTickPrerequisiteActor(Arg0);
}
static void Call5(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UActorComponent*>(B.ReadObject(M, Args[0], UActorComponent::StaticClass()));
    if (!Self) return;
    UActorComponent* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->RemoveTickPrerequisiteComponent(Arg0);
}
static void Call6(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg1, "Rotator")) return;
    Self->CacheInitialMeshOffset(Arg0, Arg1);
}
static void Call7(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    Self->Jump();
}
static void Call8(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    Self->StopJumping();
}
static void Call9(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->CanJump();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call10(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsJumpProvidingForce();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call11(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    UAnimMontage* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    FName Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    const auto Result = Self->PlayAnimMontage(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call12(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    UAnimMontage* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->StopAnimMontage(Arg0);
}
static void Call13(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetCurrentMontage();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call14(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    Self->LaunchCharacter(Arg0, Arg1, Arg2);
}
static void Call15(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->Crouch(Arg0);
}
static void Call16(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->UnCrouch(Arg0);
}
static void Call17(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->CanCrouch();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call18(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsPlayingRootMotion();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call19(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->HasAnyRootMotion();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call20(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsPlayingNetworkedRootMotionMontage();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call21(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<ACharacter*>(B.ReadObject(M, Args[0], ACharacter::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetAnimRootMotionTranslationScale();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call22(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetLastUpdateRequestedVelocity();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call23(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAvoidanceGroup(Arg0);
}
static void Call24(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetGroupsToAvoid(Arg0);
}
static void Call25(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    int32 Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetGroupsToIgnore(Arg0);
}
static void Call26(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetAvoidanceEnabled(Arg0);
}
static void Call27(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    Self->DisableMovement();
}
static void Call28(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMovementBase();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call29(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMovementBaseObject();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call30(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetImpartedMovementBaseVelocity();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call31(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 5) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    float Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    float Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[4], Arg3)) return;
    Self->CalcVelocity(Arg0, Arg1, Arg2, Arg3);
}
static void Call32(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMaxJumpHeight();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call33(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMaxJumpHeightWithJumpTime();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call34(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMinAnalogSpeed();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call35(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMaxAcceleration();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call36(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetMaxBrakingDeceleration();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call37(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetCurrentAcceleration();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Vector")) *AnswerCount = 1;
}
static void Call38(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetAnalogInputModifier();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call39(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    Self->ClearAccumulatedForces();
}
static void Call40(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    Self->SetGravityDirection(Arg0);
}
static void Call41(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetCrouchedHalfHeight(Arg0);
}
static void Call42(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetCrouchedHalfHeight();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call43(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    bool Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg1)) return;
    Self->AddImpulse(Arg0, Arg1);
}
static void Call44(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    Self->AddForce(Arg0);
}
static void Call45(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetPerchRadiusThreshold();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call46(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetValidPerchRadius();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call47(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->K2_GetWalkableFloorAngle();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call48(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetWalkableFloorAngle(Arg0);
}
static void Call49(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    const auto Result = Self->K2_GetWalkableFloorZ();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call50(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<UCharacterMovementComponent*>(B.ReadObject(M, Args[0], UCharacterMovementComponent::StaticClass()));
    if (!Self) return;
    float Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetWalkableFloorZ(Arg0);
}
static void Call51(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetControlRotation();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call52(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    FRotator Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Rotator")) return;
    Self->SetControlRotation(Arg0);
}
static void Call53(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    FVector Arg0{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg0, "Vector")) return;
    FRotator Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg1, "Rotator")) return;
    Self->SetInitialLocationAndRotation(Arg0, Arg1);
}
static void Call54(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    AActor* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg1, "Vector")) return;
    bool Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg2)) return;
    const auto Result = Self->LineOfSightTo(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call55(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    const auto Result = Self->K2_GetPawn();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call56(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetViewTarget();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call57(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    const auto Result = Self->GetDesiredRotation();
    if (LhatUEBindings::WriteValue(M, B, Result, Answers[0], "Rotator")) *AnswerCount = 1;
}
static void Call58(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsLocalController();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call59(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    APawn* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->Possess(Arg0);
}
static void Call60(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    Self->UnPossess();
}
static void Call61(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    Self->StopMovement();
}
static void Call62(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetIgnoreMoveInput(Arg0);
}
static void Call63(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    Self->ResetIgnoreMoveInput();
}
static void Call64(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsMoveInputIgnored();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call65(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    bool Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg0)) return;
    Self->SetIgnoreLookInput(Arg0);
}
static void Call66(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    Self->ResetIgnoreLookInput();
}
static void Call67(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    const auto Result = Self->IsLookInputIgnored();
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call68(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    auto* Self = static_cast<AController*>(B.ReadObject(M, Args[0], AController::StaticClass()));
    if (!Self) return;
    Self->ResetIgnoreInputFlags();
}
static void Call69(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 4) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    UBlueprint* Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    FTransform Arg2{};
    if (!LhatUEBindings::ReadValue(M, B, Args[2], Arg2, "Transform")) return;
    bool Arg3{};
    if (!LhatUEBindings::Read(M, B, Args[3], Arg3)) return;
    const auto Result = UGameplayStatics::BeginSpawningActorFromBlueprint(Arg0, Arg1, Arg2, Arg3);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call70(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetGameInstance(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call71(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetNumPlayerStates(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call72(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetPlayerState(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call73(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetNumPlayerControllers(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call74(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 1) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    const auto Result = UGameplayStatics::GetNumLocalPlayerControllers(Arg0);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call75(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetPlayerController(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call76(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetPlayerPawn(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call77(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetPlayerCharacter(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call78(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 2) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    int32 Arg1{};
    if (!LhatUEBindings::Read(M, B, Args[1], Arg1)) return;
    const auto Result = UGameplayStatics::GetPlayerCameraManager(Arg0, Arg1);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
static void Call79(LhatMachine* M, void* Context, const LhatValue* Args, size_t Count, LhatValue* Answers, int* AnswerCount)
{
    check(IsInGameThread());
    auto& B = *static_cast<FLhatBindings*>(Context);
    if (Count != 3) { LhatUEBindings::Fail(M, "Native argument count mismatch"); return; }
    UObject* Arg0{};
    if (!LhatUEBindings::Read(M, B, Args[0], Arg0)) return;
    FVector Arg1{};
    if (!LhatUEBindings::ReadValue(M, B, Args[1], Arg1, "Vector")) return;
    float Arg2{};
    if (!LhatUEBindings::Read(M, B, Args[2], Arg2)) return;
    const auto Result = UGameplayStatics::IsAnyLocalPlayerCameraWithinRange(Arg0, Arg1, Arg2);
    if (LhatUEBindings::Write(M, B, Result, Answers[0])) *AnswerCount = 1;
}
}
void Gather_LhatBundledRuntime_Engine_1(TArray<FLhatNativeType>& Types)
{
}
bool Register_LhatBundledRuntime_Engine_1(LhatProgram* P, FLhatBindings& B)
{
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "GetComponentTickInterval", "f^self^ -> number^;", Call0, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "K2_DestroyComponent", "p^self^, ue.Object|nil^;", Call1, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "AddTickPrerequisiteActor", "p^self^, ue.Actor|nil^;", Call2, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "AddTickPrerequisiteComponent", "p^self^, ue.Engine.ActorComponent|nil^;", Call3, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "RemoveTickPrerequisiteActor", "p^self^, ue.Actor|nil^;", Call4, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "ActorComponent", "RemoveTickPrerequisiteComponent", "p^self^, ue.Engine.ActorComponent|nil^;", Call5, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "CacheInitialMeshOffset", "p^self^, ue.Vector, ue.Rotator;", Call6, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "Jump", "p^self^;", Call7, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "StopJumping", "p^self^;", Call8, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "CanJump", "f^self^ -> bool^;", Call9, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "IsJumpProvidingForce", "f^self^ -> bool^;", Call10, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "PlayAnimMontage", "p^self^, ue.Engine.AnimMontage|nil^, number^, string^ -> number^;", Call11, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "StopAnimMontage", "p^self^, ue.Engine.AnimMontage|nil^;", Call12, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "GetCurrentMontage", "f^self^ -> ue.Engine.AnimMontage|nil^;", Call13, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "LaunchCharacter", "p^self^, ue.Vector, bool^, bool^;", Call14, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "Crouch", "p^self^, bool^;", Call15, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "UnCrouch", "p^self^, bool^;", Call16, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "CanCrouch", "f^self^ -> bool^;", Call17, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "IsPlayingRootMotion", "f^self^ -> bool^;", Call18, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "HasAnyRootMotion", "f^self^ -> bool^;", Call19, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "IsPlayingNetworkedRootMotionMontage", "f^self^ -> bool^;", Call20, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Character", "GetAnimRootMotionTranslationScale", "f^self^ -> number^;", Call21, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetLastUpdateRequestedVelocity", "f^self^ -> ue.Vector;", Call22, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetAvoidanceGroup", "p^self^, number^;", Call23, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetGroupsToAvoid", "p^self^, number^;", Call24, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetGroupsToIgnore", "p^self^, number^;", Call25, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetAvoidanceEnabled", "p^self^, bool^;", Call26, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "DisableMovement", "p^self^;", Call27, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetMovementBase", "f^self^ -> ue.Engine.PrimitiveComponent|nil^;", Call28, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetMovementBaseObject", "f^self^ -> ue.Object|nil^;", Call29, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetImpartedMovementBaseVelocity", "f^self^ -> ue.Vector;", Call30, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "CalcVelocity", "p^self^, number^, number^, bool^, number^;", Call31, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetMaxJumpHeight", "f^self^ -> number^;", Call32, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetMaxJumpHeightWithJumpTime", "f^self^ -> number^;", Call33, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetMinAnalogSpeed", "f^self^ -> number^;", Call34, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetMaxAcceleration", "f^self^ -> number^;", Call35, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetMaxBrakingDeceleration", "f^self^ -> number^;", Call36, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetCurrentAcceleration", "f^self^ -> ue.Vector;", Call37, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetAnalogInputModifier", "f^self^ -> number^;", Call38, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "ClearAccumulatedForces", "p^self^;", Call39, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetGravityDirection", "p^self^, ue.Vector;", Call40, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetCrouchedHalfHeight", "p^self^, number^;", Call41, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetCrouchedHalfHeight", "f^self^ -> number^;", Call42, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "AddImpulse", "p^self^, ue.Vector, bool^;", Call43, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "AddForce", "p^self^, ue.Vector;", Call44, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetPerchRadiusThreshold", "f^self^ -> number^;", Call45, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "GetValidPerchRadius", "f^self^ -> number^;", Call46, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "K2_GetWalkableFloorAngle", "f^self^ -> number^;", Call47, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetWalkableFloorAngle", "p^self^, number^;", Call48, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "K2_GetWalkableFloorZ", "f^self^ -> number^;", Call49, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "CharacterMovementComponent", "SetWalkableFloorZ", "p^self^, number^;", Call50, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "GetControlRotation", "f^self^ -> ue.Rotator;", Call51, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "SetControlRotation", "p^self^, ue.Rotator;", Call52, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "SetInitialLocationAndRotation", "p^self^, ue.Vector, ue.Rotator;", Call53, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "LineOfSightTo", "f^self^, ue.Actor|nil^, ue.Vector, bool^ -> bool^;", Call54, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "K2_GetPawn", "f^self^ -> ue.Engine.Pawn|nil^;", Call55, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "GetViewTarget", "f^self^ -> ue.Actor|nil^;", Call56, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "GetDesiredRotation", "f^self^ -> ue.Rotator;", Call57, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "IsLocalController", "f^self^ -> bool^;", Call58, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "Possess", "p^self^, ue.Engine.Pawn|nil^;", Call59, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "UnPossess", "p^self^;", Call60, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "StopMovement", "p^self^;", Call61, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "SetIgnoreMoveInput", "p^self^, bool^;", Call62, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "ResetIgnoreMoveInput", "p^self^;", Call63, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "IsMoveInputIgnored", "f^self^ -> bool^;", Call64, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "SetIgnoreLookInput", "p^self^, bool^;", Call65, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "ResetIgnoreLookInput", "p^self^;", Call66, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "IsLookInputIgnored", "f^self^ -> bool^;", Call67, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "Controller", "ResetIgnoreInputFlags", "p^self^;", Call68, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "BeginSpawningActorFromBlueprint", "p^ue.Object|nil^, ue.Engine.Blueprint|nil^, ue.Transform, bool^ -> ue.Actor|nil^;", Call69, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetGameInstance", "f^ue.Object|nil^ -> ue.Engine.GameInstance|nil^;", Call70, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetNumPlayerStates", "f^ue.Object|nil^ -> number^;", Call71, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlayerState", "f^ue.Object|nil^, number^ -> ue.Engine.PlayerState|nil^;", Call72, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetNumPlayerControllers", "f^ue.Object|nil^ -> number^;", Call73, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetNumLocalPlayerControllers", "f^ue.Object|nil^ -> number^;", Call74, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlayerController", "f^ue.Object|nil^, number^ -> ue.Engine.PlayerController|nil^;", Call75, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlayerPawn", "f^ue.Object|nil^, number^ -> ue.Engine.Pawn|nil^;", Call76, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlayerCharacter", "f^ue.Object|nil^, number^ -> ue.Engine.Character|nil^;", Call77, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "GetPlayerCameraManager", "f^ue.Object|nil^, number^ -> ue.Engine.PlayerCameraManager|nil^;", Call78, &B)) return false;
    if (!lhat_register_member(P, "ue.Engine", "GameplayStatics", "IsAnyLocalPlayerCameraWithinRange", "f^ue.Object|nil^, ue.Vector, number^ -> bool^;", Call79, &B)) return false;
    return true;
}
PRAGMA_ENABLE_DEPRECATION_WARNINGS
